{
  "adapter": "convex",
  "generatedBy": "ledger:update (PROTOTYPE: split verbatim from actions.json)",
  "expectedUnsupportedMessages": {
    "matches-alt": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "filter-as-condition": "filter() returns a list, not a boolean, so it cannot be a condition on its own; only size() over its result has a boolean meaning",
    "filter-as-conjunct": "filter() returns a list, not a boolean, so it cannot be a condition on its own; only size() over its result has a boolean meaning",
    "map-as-condition": "map() returns a list, not a boolean, so it cannot be a condition on its own; only size() over its result has a boolean meaning"
  },
  "nullRepresentationOmittedMessages": {
    "null-eq-missing": "missing-attribute error"
  },
  "adapterSupportedExpected": [
    {
      "action": "null-value-f2f-mixed",
      "reason": "Convex stores the value the caller sent, so a stored null compares as a null VALUE and a stored-null field is distinguishable from an absent one. The mixed conventions the SQL adapters cannot render in one predicate are both directly representable here"
    },
    {
      "action": "p-index",
      "reason": "Convex queries can apply the adapter's explicit post-filter for list indexing"
    },
    {
      "action": "p-matches",
      "reason": "Convex queries can apply the adapter's explicit post-filter for regular-expression matching"
    },
    {
      "action": "p-timestamp",
      "reason": "Convex queries can apply the adapter's explicit post-filter for timestamp comparisons"
    },
    {
      "action": "cast-int-string",
      "reason": "The adapter's explicit post-filter reimplements CEL int() over strings exactly (whole-string parse or error-deny), so the SQL leading-digits divergence does not apply"
    },
    {
      "action": "cast-double-string",
      "reason": "The adapter's explicit post-filter reimplements CEL double() over strings exactly (whole-string parse or error-deny), so the SQL leading-digits divergence does not apply"
    },
    {
      "action": "cast-int-double",
      "reason": "The adapter's explicit post-filter reimplements CEL int() truncation toward zero, so the SQL CAST round-to-nearest divergence does not apply"
    }
  ],
  "reasons": {
    "Unsupported operator: except": [
      "The Convex adapter refuses this shape during structural validation: Unsupported operator: except."
    ],
    "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset": [
      "Lookahead is outside RE2 and the Convex evaluator\u2019s supported literal, anchor, and trailing wildcard subset. ",
      "The Convex adapter refuses this shape during structural validation: matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset."
    ],
    "division requires a constant denominator: the plan does not preserve numeric types needed to distinguish integer errors from floating-point infinity": [
      "The Convex adapter refuses this shape during structural validation: division requires a constant denominator: the plan does not preserve numeric types needed to distinguish integer errors from floating-point infinity."
    ],
    "Unsupported operator: struct": [
      "The Convex adapter refuses this shape during structural validation: Unsupported operator: struct."
    ],
    "Unsupported operator: list": [
      "The Convex adapter refuses this shape during structural validation: Unsupported operator: list.",
      "The descendant path is CONSTRUCTED by list() from a constant segment and the primary key. `list` is not in the adapter's known-operator set at all \u2014 it has never reached the wire from this corpus \u2014 so the shape is refused during structural validation, before the hierarchy operators around it are considered"
    ],
    "division by a constant zero whose sign is indeterminate: a query plan is JSON-encoded on its way into a Convex function and JSON has no -0, so the adapter cannot tell +Infinity from -Infinity": [
      "IEEE keeps the sign of a zero, and the planner does ship it (the wire operand for -0.0 is `-0`), but a query plan is JSON-encoded on its way into a Convex function and JSON.stringify(-0) is \"0\" \u2014 the sign bit is gone before the adapter sees it, so it rejects a constant zero denominator rather than guess which infinity CEL produced",
      "Same indeterminate-signed-zero rejection as cr-div-neg-zero: the 1.0/0.0 and -1.0/0.0 branches also carry a constant zero denominator, which survives the JSON hop as an unsigned 0"
    ]
  },
  "refuses": {
    "cr-div-neg-zero": "division by a constant zero whose sign is indeterminate: a query plan is JSON-encoded on its way into a Convex function and JSON has no -0, so the adapter cannot tell +Infinity from -Infinity",
    "div-by-division": "division requires a constant denominator: the plan does not preserve numeric types needed to distinguish integer errors from floating-point infinity",
    "eq-map": "Unsupported operator: struct",
    "eq-map-null": "Unsupported operator: struct",
    "except-eq": "Unsupported operator: except",
    "except-root": "Unsupported operator: except",
    "except-size": "Unsupported operator: except",
    "hasint-map-element": "Unsupported operator: list",
    "hier-list-id": "Unsupported operator: list",
    "hier-overlaps-list-prefix": "Unsupported operator: list",
    "in-nested-list": "Unsupported operator: list",
    "nan-ord-inf": "division by a constant zero whose sign is indeterminate: a query plan is JSON-encoded on its way into a Convex function and JSON has no -0, so the adapter cannot tell +Infinity from -Infinity",
    "ne-map": "Unsupported operator: struct",
    "pv-except": "Unsupported operator: except",
    "pv-structs": "Unsupported operator: list",
    "pv-structs-null": "Unsupported operator: list",
    "regex-alternation": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-brace": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-case": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-digit": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-dot": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-grouped": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-lookahead": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-optional-operators": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-posix": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset",
    "regex-repetition": "matches requires a constant RE2-compatible pattern in the supported literal, anchor, and trailing .* subset"
  }
}